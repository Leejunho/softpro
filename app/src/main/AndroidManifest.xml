<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullScreenTheme">
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.reset_password"/>
        <activity android:name=".activity.profileActivity" />
        <activity android:name=".activity.AddinfoActivity" />
        <activity android:name=".activity.NoticeboardActivity" />
        <activity android:name=".activity.DeliverystatusActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.CameraActivity" />
        <activity android:name=".fragment.Camera2BasicFragment" />
        <activity android:name=".activity.GalleryActivity" />
        <activity android:name=".activity.BasicActivity" />
        <activity android:name=".activity.WritePostActivity" />
        <activity android:name=".activity.PostActivity" />
        <activity android:name=".activity.CheckpostActivity" />
        <activity android:name=".activity.ChatRoomActivity" />
        <activity android:name=".activity.ChatActivity" />
        <activity android:name=".activity.SelectUserActivity" />
        <activity android:name=".activity.DevelopuserActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>